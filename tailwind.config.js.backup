/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{rs,html,css}",
    "./dist/**/*.html",
    "./index.html",
    // Scan Rust files for Dioxus class syntax
    "./src/**/*.rs"
  ],
  // Safelist classes that Tailwind might not detect in Dioxus RSX syntax
  safelist: [
    'text-gray-600',
    'dark:text-gray-400',
    // Add more classes here as needed when Tailwind doesn't detect them
  ],
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [],
}